import { Meta, Story } from "@storybook/blocks";

import { DefaultStory } from "./ProcessNavigation.stories";

<Meta title="Modules/ProcessNavigation/Documentation" />

# Overview

// TODO sub process navigation
// TODO called elements navigation
// TOOD ui extension with navigation elements
// TODO Disclaimer that this module currently does not update the ui on element changes via editing

# Configuration

The `Process Navigation Module` can be added to any `BaseViewer` instance via the `additionalModules` config property.

```js
import CoreModule from 'bpmn-js/lib/core';
import { useBaseViewer } from "bpmn-react/Viewer"
import { ProcessNavigationModule } from "bpmn-react/Modules/ProcessNavigation"

const BaseViewer = ({ xml }) => {
    const [handleViewerRef, viewer] = useBaseViewer({ additionalModules: [ CoreModule, ProcessNavigationModule ] });

    useEffect(() => {
        if (xml) {
            viewer?.importXML(xml);
        }
    }, [viewer, xml])

    return <div ref={ handleViewerRef } />
}
```

// TODO customizing the control and overlay renderer

# Examples

<Story of={ DefaultStory } />
